version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --legacy-peer-deps
    build:
      commands:
        - npm run build -- --webpack

  artifacts:
    baseDirectory: out
    files:
      - '**/*'

  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
